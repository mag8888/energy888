[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build:minimal && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
NODE_OPTIONS = "--max-old-space-size=8192"
NODE_ENV = "production"
